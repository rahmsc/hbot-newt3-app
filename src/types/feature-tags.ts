export type FeatureTag = {
  id: string;
  label: string;
  iconPath: string;
  description: string;
  images: {
    topHorizontal: string;
    bottomHorizontal: string;
    vertical: string;
  };
};
